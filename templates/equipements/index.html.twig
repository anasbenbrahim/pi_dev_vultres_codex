{% extends 'baseclient.html.twig' %}

<<<<<<< HEAD
{% block title %}Nos Equipements{% endblock %}

{% block body %}
{% block css %}
    <style>
    .card-link {
    text-decoration: none;
    color: inherit;
}
    .card {
    width: 100%;
    max-width: 900px;
    margin: 20px auto;
    background: #fff;
    border-radius: 10px;
    box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.1);
    overflow: hidden;
    transition: transform 0.3s ease-in-out;
    display: flex;
    flex-direction: column;
}

.card:hover {
    transform: scale(1.02);
}

/* Ensure the image is fully contained */
.card img {
    width: 100%;
    height: auto; /* Ensures aspect ratio is maintained */
    object-fit: cover;
    display: block;
}

/* Card Content */
.card-content {
    padding: 20px;
    text-align: center;
    flex: 1;
}

.card-title {
    font-size: 24px;
    font-weight: bold;
    margin-bottom: 10px;
}

.card-price {
    font-size: 20px;
    color: #28a745;
    margin-bottom: 10px;
}

.card-description {
    font-size: 16px;
    color: #6c757d;
    margin-bottom: 15px;
}

.btn {
    display: inline-block;
    text-decoration: none;
    background: #007bff;
    color: #fff;
    padding: 12px 20px;
    border-radius: 5px;
    transition: background 0.3s ease-in-out;
}

.btn:hover {
    background: #0056b3;
}

/* Responsive Design */
@media (min-width: 600px) {
    .card {
        flex-direction: row;
        align-items: center;
    }

    .card img {
        width: 50%;
        height: 100%;
        object-fit: cover; /* Ensures image fills the container */
    }

    .card-content {
        width: 50%;
        text-align: left;
        padding: 30px;
    }
}

    </style>
{% endblock %}

    {% for equipement in list%}
        <a href="#" class="card-link">  
            <div class="card">
                <img src="{{asset('uploads/images/' ~ equipement.image)}}">
                <div class="card-content">
                    <div class="card-title">{{equipement.nom}}</div>
                    <div class="card-price">{{equipement.prix}}</div>
                    <div class="card-description">{{equipement.description}}</div>
                    <a href="{{path('show_detail',{'id':equipement.id})}}" class="btn">Buy Now</a>
                </div>
            </div>
        </a>
    {% endfor %}
    
{% endblock %}
=======
{% block title %}Hello ProductsController!{% endblock %}
{% block footer %}{% endblock %}

{% block body %}

<style>


</style>

          
<div class="container mx-auto mt-4">
    <div class="row">
     {% if list is not empty %}
    {% for equipement in list %}
        <div class="col-md-4">
            <div class="card shadow" style="width: 18rem;">
            <div class="">
                <img src="{{asset('uploads/images/' ~ equipement.image)}}" class="card-img-top" alt="...">
                <hr>
                </div>
                <div class="card-body">
                    <h4 class="card-title text-center">{{equipement.nom}} <span class="ms-5">{{equipement.prix}} DT</span></h4>
                   
                    <p class="card-text mb-3">{{equipement.description}}</p>
                    <a href="{{path('show_detail',{'id':equipement.id})}}"  class="btn btn-warning"><i class="fas fa-link"></i> Buy Now</a>
                    
                </div>
            </div>
        </div>
        
                  {% endfor %}
{% endif %}
       

        
        
        
        
    </div>
</div>


{% endblock %}



















>>>>>>> integration
